{
  "name": "pinchards-is",
  "private": true,
  "scripts": {
    "vendor:frontend": "node scripts/vendor-frontend.js",
    "vendor": "composer install && npm run vendor:frontend",
    "vendor:prod": "composer install --no-dev --optimize-autoloader && npm run vendor:frontend"
  },
  "devDependencies": {
    "bootstrap": "5.3.8"
  },
  "dependencies": {
    "gsap": "^3.15.0"
  }
}
